# T-026: Кнопка/почта справа, пустышка дашборд, редирект на «Загрузить» (итерация 4)

**Владелец:** основной агент
**Дата:** 2026-09-05T10:40:00Z
**Ветка:** `feature/adaptive-header`
**Предыдущая попытка:** `docs/agent/tasks/T-026/20260905T102500Z_main_a03.md`

## Задача

1. Кнопка «Выйти» и почта — справа в углу шапки.
2. Пустышка «Дашборд» по центру, справа от «Загрузить документ».
3. Логотип ведёт на этот дашборд.
4. После входа и после регистрации перенаправлять на вкладку «Загрузить документ».

## Что сделано

- `frontend/src/components/Layout.js`:
  - Блок `.user-info` (email + «Выйти») — уже в правом углу (`justify-self: end`), оставлен.
  - Логотип ссылается на `/dashboard` (`<Link to="/dashboard">`).
  - В центр-нав добавлена ссылка «Дашборд» справа от «Загрузить»:
    `Создать семью | Загрузить | Дашборд`.
- `frontend/src/pages/DashboardStub.js` (новый): пустая страница-заглушка
  дашборда с пояснением, что аналитика появится после загрузки выписки.
- `frontend/src/App.js`: добавлен маршрут `/dashboard` (PrivateRoute → Layout →
  DashboardStub).
- `frontend/src/pages/Login.js` и `frontend/src/pages/Register.js`:
  после успешного входа/регистрации `navigate('/upload')` вместо `navigate('/')`.

## Результат проверки

- `npm run check` — Compiled successfully.
- Ручной сценарий в браузере: вход → попадаем на `/upload`; регистрация →
  попадаем на `/upload`; клик по логотипу → `/dashboard` (заглушка);
  в шапке по центру: «Создать семью | Загрузить | Дашборд», справа —
  email + «Выйти».

## Точный следующий шаг

Ручная проверка в браузере после `docker compose up --build` сценария выше.