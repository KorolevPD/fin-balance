# T-072: Миграция AI-ассистента с Gemini на GigaChat

- Дата: 2026-09-07T14:24:50Z
- Владелец: opencode (основной агент)
- Ветка: feature/T-072-gigachat
- Предыдущая попытка: нет (новая задача; аналог по структуре — T-068, T-069)

## Точный следующий шаг из предыдущей попытки

Не применимо — задача новая.

## Задача пользователя

1. Поменять интеграцию AI-ассистента с Gemini на GigaChat.
2. Убрать все другие интеграции (Gemini, OpenAI-совместимые) — только GigaChat.
3. В профиле предлагать пользователю загрузить только данные для интеграции с
   GigaChat, если нет глобального API-ключа в env. Если ключ есть в env — раздел
   «AI-ассистент» в профиле не выводится вовсе.
4. Если GigaChat API-ключ есть в env, на дашборде должна быть возможность
   получить совет от AI (сейчас выводится «Добавьте бесплатный AI-ключ в профиле...»).
5. Переименовать .env-переменные, связанные с ИИ-функционалом.

## Решения пользователя (уточнены в чате)

- `GEMINI_API_KEY` → `GIGACHAT_API_KEY`.
- `AI_KEY_ENCRYPTION_KEY` оставить как есть (нейтральное имя, не привязано к провайдеру).
- Поле в профиле принимает **Authorization Key** GigaChat; бэкенд по нему получает
  временный access-токен (OAuth, 30 мин) и вызывает `chat/completions`.

## Особенности GigaChat API (по официальной документации, авг-окт 2026)

- Токен доступа: `POST https://ngw.devices.sberbank.ru:9443/api/v2/oauth`,
  form: `scope=GIGACHAT_API_PERS`, заголовки: `RqUID: <uuid4>`,
  `Authorization: Basic|Bearer <ключ_авторизации>`. Ответ: `{"access_token": ...}`.
- Генерация: `POST https://api.giga.chat/v1/chat/completions` (OpenAI-формат),
  заголовок `Authorization: Bearer <access_token>`, тело `{"model": "GigaChat",
  "messages": [...]}`, `stream: false`.
- Модель по умолчанию: `GigaChat`.
- Документация не рекомендует передавать `temperature`/`top_p` — опускаем их.

## План правок

- `backend/app/ai/gigachat.py` — новый провайдер (OAuth-токен + chat/completions).
- Удалить `backend/app/ai/gemini.py`, `backend/app/ai/openai_compat.py`.
- `backend/app/ai/client.py` — `PROVIDER_GIGACHAT = "gigachat"`, DEFAULT_PROVIDER,
  normalized_provider/is_supported только gigachat, `_load` → gigachat.
- `backend/app/ai/server_key.py` — `GIGACHAT_API_KEY`, `server_gigachat_key()`,
  `has_server_gigachat_key()`.
- `backend/app/ai/__init__.py` — экспорты под gigachat.
- `backend/app/routers/ai.py`, `backend/app/services/ai_transactions.py` —
  серверный ключ GigaChat, provider `"gigachat"`.
- `backend/app/routers/auth.py` — `has_server_gigachat_key()`.
- Конфиги: `.env.example`, `backend/.env.example`, `docker-compose.yml`,
  `deploy/deploy.sh`, `.github/workflows/deploy.yml`, `deploy/DEPLOY.md`.
- Frontend: `Profile.js` — раздел «AI-ассистент» скрыт при `server_ai_key`;
  без серверного ключа — только поле GigaChat Authorization Key (без выбора
  провайдера и base_url). `Dashboard.js` — советы доступны при серверном ключе
  или ключе пользователя (`server_ai_key || has_ai_key`), текст сообщения — GigaChat.
- Тесты: `tests/test_ai.py`, `tests/test_ai_advices.py`, `tests/test_profile.py`.

## Проверка

- `pytest` (новые/обновлённые тесты про GigaChat, OAuth-токен, серверный ключ).
- `ruff check` по изменённым файлам.
- `npm run check` (сборка фронтенда).