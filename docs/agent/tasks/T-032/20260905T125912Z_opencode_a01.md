# T-032: Кликабельное имя пользователя в шапке

**Владелец:** opencode
**Дата:** 2026-09-05T12:59:12Z
**Попытка:** a01
**Предыдущая попытка:** нет (задача новая)
**Git HEAD до работы:** main (весь профиль T-031 уже смёржен)

## Задача

В шапке (Layout) убрать отдельный пункт навигации «Профиль» и сделать
кликабельным имя пользователя — клик ведёт на страницу `/profile`.

## Разрешённая область

- `frontend/src/components/Layout.js`
- `frontend/src/styles.css` (при необходимости)
- Пересборка `frontend/build/` (артефакты деплоя)
- `TASKS.md`, timestamp-файл

## Запрещено

- Изменение backend, роутов, AuthContext, страницы Profile.
- Новые зависимости.

## Проверяемые критерии

- [ ] Пункт «Профиль» убран из `nav`.
- [ ] Имя пользователя в `.user-info` становится ссылкой на `/profile`.
- [ ] `npm run check` — Compiled successfully.

## Выполненные действия

- [x] Убрал `<Link to="/profile">Профиль</Link>` из `nav`.
- [x] Заменил `<span>` на `<Link to="/profile">` вокруг имени пользователя
      (класс `user-info-name`, `title="Мой профиль"`).
- [x] Добавил стили `.user-info-name` (ссылочный вид, hover, ellipsis).
- [x] В мобильном правиле заменено скрытие `.user-info span` на `.user-info-name`.
- [x] Пересобрал `frontend/build/`.

## Команды и результаты

```text
Команда: npm run check
Результат: Compiled successfully
```

## Ручная проверка

Карточка задачи — интерфейсная. См. `docs/rules/frontend.md`: пункт «Профиль»
в nav убран, имя пользователя — кликабельная ссылка на `/profile`. Проверка
компиляции подтверждена `npm run check`. Аватар в шапке остался кликабельным
на `/profile`.

## Блокеры и риски

Нет.

## Точный следующий шаг

Координатор: проверить diff, затем ветка + PR.
