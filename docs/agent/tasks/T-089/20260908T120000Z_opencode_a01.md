# T-089 (a01): обновление README.md — переменные окружения и авторы

- Дата: 2026-09-08T12:00:00Z
- Владелец: opencode
- Ветка: feature/T-089-readme-env-authors

## Задача

1. Обновить описания переменных окружения в README.md.
2. Добавить раздел «Авторы» (7 человек с ролями и GitHub-логинами).

## Анализ переменных

Сверка README с фактическим кодом и `.env.example`:

- `POSTGRES_USER`, `POSTGRES_PASSWORD`, `POSTGRES_DB` — есть;
- `SECRET_KEY`, `AI_KEY_ENCRYPTION_KEY`, `GIGACHAT_API_KEY` — есть;
- `BOT_TOKEN`, `API_URL`, `DEPLOY_PORT`/`DEPLOY_PORT_SSL` — есть;
- `GIGACHAT_CA_BUNDLE` — **отсутствовала** в таблице README, при этом
  читается в `backend/app/ai/gigachat.py:41` (`_CA_ENV`) и описана в
  корневом и backend `.env.example`;
- «Дополнительные настройки backend» (DATABASE_URL, ALGORITHM,
  ACCESS_TOKEN_EXPIRE_MINUTES) — описаны, актуальны.

## Изменения README.md

1. В таблицу «Переменные окружения» добавлена строка `GIGACHAT_CA_BUNDLE`
   (корневой сертификат НУЦ Минцифры для TLS к GigaChat; пустой =
   стандартный trust-store; TLS не отключает).
2. В конец файла добавлен раздел «## Авторы»:
   - Королев Павел @korolevpd — тимлид, бэкенд;
   - Симонов Михаил @ludovolk6666 — аналитик;
   - Прокопенко Валерия @wfxxyyy — дизайнер;
   - Болотов Яромир @KonoLobster — фронтенд/дизайн;
   - Иванов Илья @TT_AZART — промыт;
   - Попова Екатерина @ritritsu — фронтенд/дизайн;
   - Туржевский Дмитрий @hsdhhdjdjdhd — бэкенд.

## Проверка

- VCS-проверка diff; markdown не компилируется (документация).