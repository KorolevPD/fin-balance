# T-078 — Замена цвета #5b5bea на #24a647 (hover/UI-элементы)

**Владелец:** основной агент
**Дата:** 2026-09-07T18:49:52Z
**Статус:** IN_PROGRESS

## Цель

Заменить цвет `#5b5bea` на `#24a647` в элементах интерфейса, которые
подсвечиваются при наведении (и сопутствующих UI-элементах с этим цветом).

## Ограничения (по задаче)

- **Категорически не трогать** круговую диаграмму «Траты по категориям».
  `CATEGORY_COLORS` в `frontend/src/pages/Dashboard.js:24` (содержит `#5b5bea`) —
  НЕ изменялся.
- Пользователь подтвердил: столбчатую диаграмму «Динамика расходов»
  (`fill="#5b5bea"`) менять можно → изменена на `#24a647`.

## Файлы и элементы

### frontend/src/styles.css
1. `.form-group input:focus` — `border-color` (#5b5bea → #24a647) + box-shadow
   `rgb(91 91 234 / 15%)` → `rgb(36 166 71 / 15%)`.
2. `.auth-link a` — `color` (ссылки «Войти / Зарегистрироваться»).
3. `.user-info-name:hover` — `color` (имя пользователя в шапке при наведении).
4. `.family-list a:hover .family-name` — `color` (название семьи при наведении).
5. `.upload-zone.drag-over` — `border-color` (зона загрузки: наведение + drag).
6. `.upload-zone:hover` — `border-color` (зона загрузки при наведении).
7. `.upload-icon` — `color` (иконка загрузки).
8. `.upload-reset:hover` — `border-color` + `color` (кнопка «Сбросить» при наведении).
9. `.progress-bar` — `background` (полоса прогресса загрузки).
10. `.card-header-link` — `color` (ссылки в шапках карточек).
11. `.sort-btn:hover` — `color` (заголовки сортировки таблиц при наведении).
12. `.sort-indicator.active` — `color` (активный индикатор сортировки).
13. `.percent-bar` — `background` (мини-бар процентов в таблице категорий).
14. `.modal select:focus` — `border-color` + box-shadow (фокус селекта в модалке).
15. `.avatar-placeholder` — `background` (заглушка аватара в профиле).
16. `.header-avatar` — `background` (аватар в шапке).

### frontend/src/pages/Dashboard.js
- Строка 765: `<Bar dataKey="amount" fill="#5b5bea">` → `fill="#24a647"`
  (столбчатая диаграмма «Динамика расходов» — разрешено пользователем).
- Строка 24: `CATEGORY_COLORS` (круговая диаграмма) — НЕ изменялась.

## Проверка

- `npm run check` — Compiled successfully.
- Проверить grep: в `frontend/src/` остаётся единственное `#5b5bea` в
  `Dashboard.js:24` (круговая диаграмма).