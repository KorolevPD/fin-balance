# Файл выполнения задачи

ID выполнения: 20260905T071835Z_main_a01
Задача: T-019
Исполнитель: main
Начало: 2026-09-05T07:18:35Z
Предыдущая попытка: нет
Git HEAD до работы: 4cfe23d (origin/main, ветка feature/T-019-nginx-reverse-proxy)
Статус: DONE

## Цель выполнения

Настроить nginx как единую точку входа: статическая раздача фронтенда
(React SPA) с fallback на `index.html`, проксирование `/api` и `/bot`
на backend без потери префикса.

## Разрешённая область

- `nginx/nginx.conf`
- `nginx/Dockerfile`
- `docker-compose.yml` — только контекст сборки сервиса nginx (иначе
  Dockerfile не соберётся, контекст `./nginx` не содержит frontend).
- `TASKS.md`
- `docs/agent/tasks/T-019/`

## Запрещено

- Изменения кода backend/frontend/bot.
- Новые зависимости.
- Переписывание остальных сервисов `docker-compose.yml`.

## Проверяемые критерии

- [x] nginx отдаёт статический фронтенд из `/usr/share/nginx/html`: `/`
      возвращает `index.html`, а маршруты SPA (`/login`) отдают тот же
      `index.html` (fallback клиентского роутера).
- [x] `/api/...` проксируется на backend:8000 с сохранением префикса
      `/api` (роуты FastAPI зарегистрированы с префиксом `/api`).
- [x] `/bot/...` проксируется на backend:8000 с сохранением префикса
      `/bot` (webhook-эндпоинт бота).

## Выполненные действия

- Ветка `feature/T-019-nginx-reverse-proxy` создана от `origin/main`
  (4cfe23d), чтобы PR содержал только изменения T-019 (ветка T-010
  ещё не влита в main).
- `nginx/nginx.conf` переписан:
  - удалён апстрим `frontend` (фронтенд отдаёт сам nginx, статика);
  - добавлены mime-типы, `sendfile`, `tcp_nopush`, `server_tokens off`, gzip;
  - `location /api/` и `location /bot/` → `proxy_pass http://backend;`
    (без URI) — префикс сохраняется, исправлен баг старой конфигурации,
    где `proxy_pass http://backend/;` срезал `/api` и ломал маршруты
    FastAPI;
  - `location = /health` → backend `/health`;
  - `location /static/` — кеширование хешированных ассетов сборки;
  - `location /` — `try_files $uri $uri/ /index.html;` (SPA fallback).
- `nginx/Dockerfile` переведён на multi-stage:
  - stage 1: сборка фронтенда (`npm run build`) с
    `REACT_APP_API_URL=/api`, чтобы прод-сборка ходила в API через nginx
    (same-origin);
  - stage 2: nginx:1.25-alpine + `nginx.conf` + статика в
    `/usr/share/nginx/html`.
- `docker-compose.yml`: для сервиса nginx контекст сборки изменён
  с `./nginx` на `.`, dockerfile `nginx/Dockerfile` (требуется доступ
  к исходникам frontend на этапе сборки).
- Проверка описана в разделе «Команды и результаты». Docker-проверка
  не выполнялась: Docker Desktop не может запустить демон без WSL2
  (WSL2 не установлен на машине).

## Изменённые файлы

- `nginx/nginx.conf` — переписан.
- `nginx/Dockerfile` — multi-stage со сборкой фронтенда.
- `docker-compose.yml` — контекст сборки сервиса nginx.
- `TASKS.md` — статус T-019 → DONE.
- `docs/agent/tasks/T-019/20260905T071835Z_main_a01.md` — файл выполнения.

## Команды и результаты

```text
Команда: docker build ... / docker run ... / nginx -t
Результат: НЕ ВЫПОЛНЯЛОСЬ. Docker-демон недоступен (Docker Desktop
не стартует: WSL2 не установлен). Верификация выполнена локально:

Команда: npm ci && REACT_APP_API_URL=/api npm run build
        (в /tmp/opencode/t019-frontend, копия frontend/)
Результат: сборка успешна; build/index.html + build/static/js|css
с хешированными именами (main.d8d4d93c.css, main.e28e49b7.js).

Команда: python -c "проверка структуры nginx.conf"
Результат: скобки сбалансированы; присутствуют location /api/, /bot/,
=/health, /static/, /; try_files $uri $uri/ /index.html; proxy_pass
http://backend; (без среза префикса); upstream backend; root;
mime.types; server_tokens off. — ALL STATIC CHECKS PASSED.
```

Сквозную проверку (`docker compose up`, curl через nginx на реальном
backend) выполнит T-020, в чьей области «Интеграция всех сервисов,
проверка docker compose up».

## Блокеры и риски

- Docker-проверка (nginx -t, curl через собранный образ) недоступна
  на этой машине: Docker Desktop не запускается без установленного
  WSL2. Конфиг проверен статическим анализом, стадия сборки фронта —
  локальным `npm run build`. Полную проверку через `docker compose up`
  выполнит T-020.
- Webhook-эндпоинта `/bot` на backend пока нет (бот работает на
  long-polling), nginx-сторона готова; роут появится в T-016–T-018.
- Контекст сборки nginx в docker-compose изменён на корень репозитория —
  это минимальная правка, без неё multi-stage Dockerfile не соберётся.

## Точный следующий шаг

Задача завершена: сформировать коммиты, запушить ветку
`feature/T-019-nginx-reverse-proxy` и создать PR на проверку координатором.

## SUBAGENT_HANDOFF

**Статус:** DONE
**Выполненные критерии:** 3/3
**Оставшиеся критерии:** нет
**Изменённые файлы:** `nginx/nginx.conf`, `nginx/Dockerfile`,
`docker-compose.yml`, `TASKS.md`, `docs/agent/tasks/T-019/20260905T071835Z_main_a01.md`
**Команды и результаты:** см. раздел «Команды и результаты» выше.
**Блокеры:** Docker-демон недоступен (WSL2 не установлен) — конфиг
проверен статически, сборка фронта проверена локально; сквозную
проверку выполнит T-020.
**Точный следующий шаг:** коммиты + пуш + PR; далее T-020 —
проверка `docker compose up`.