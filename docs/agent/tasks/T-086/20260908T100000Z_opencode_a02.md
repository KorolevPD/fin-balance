# T-086 (a02): разрешение конфликтов PR #119 при merge origin/main

- Дата: 2026-09-08T10:00:00Z
- Владелец: opencode
- Ветка: feature/T-086-simplify-family-block
- Предыдущая попытка: 20260908T080000Z_opencode_a01.md

## Ситуация

Пока PR #119 (T-086) был открыт, в origin/main смержен PR #120 (T-087:
favicon-набор и `<title>ФинБаланс</title>`). Возникли conflicts:

- `frontend/src/pages/Profile.js`, `frontend/src/styles.css` — автослияние
  без маркеров (0 конфликтов); функциональность T-086 на месте:
  `family-actions-row`, `family-code`, `copy-btn`, `family-join-fields`
  (4 использования в JSX, 10 в CSS).
- T-087 на месте: `public/index.html` содержит `<title>ФинБаланс</title>`.
- `frontend/build/*` — rename/rename, rename/delete, modify/delete:
  генерируемые артефакты; склейка вручную бессмысленна.

## Разрешение

1. `git rm -rf frontend/build` — каталог удалён; `git diff --diff-filter=U`
   пуст.
2. Полная пересборка `npm run check` — новый согласованный `frontend/build`
   с функциональностью обеих веток (профиль T-086 + favicon/title T-087).
3. `git add -A frontend/build`, затем merge-commit
   «Merge origin/main into feature/T-086-simplify-family-block (resolve
   build/ conflicts)».

## Проверки

- `npm run check` — Compiled successfully.
- `CI=true npx react-scripts test` — 2 passed.
- Проверка артефактов после сборки: favicon.*, build/index.html
  с `<title>ФинБаланс</title>`.

## Результат

После push GitHub покажет mergeable = CLEAN; запрашивается повторный review.
Замечание: backend не затронут — изменения только фронтенд и build.