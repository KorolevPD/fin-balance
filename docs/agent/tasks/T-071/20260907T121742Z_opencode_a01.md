# Файл выполнения задачи

ID выполнения: 20260907T121742Z_opencode_a01
Задача: T-071
Исполнитель: основной агент
Начало: 2026-09-07T12:17:42Z
Предыдущая попытка: нет
Git HEAD до работы: cf8d4df
Статус: IN_PROGRESS

## Цель выполнения

Ускорить деплой на слабый сервер (1 vCPU / 1 ГБ RAM / 10 ГБ диск): перенести
сборку фронтенда (CRA) с сервера на GitHub Actions runner. На сервере nginx-образ
собирается из готовых статиков `frontend/build` без node-стадии.

## Разрешённая область

- `nginx/Dockerfile.prod` (новый, тонкий образ для прода)
- `docker-compose.prod.yml` (nginx build → Dockerfile.prod)
- `.github/workflows/deploy.yml` (сборка SPA на runner перед scp)
- `deploy/DEPLOY.md` (описание нового потока)
- `docs/agent/tasks/T-071/`

## Запрещено

- Несвязанные изменения.
- Новые зависимости без одобрения.

## Проверяемые критерии

- [x] `nginx/Dockerfile.prod` не содержит node-стадию и копирует готовый
      `frontend/build` в `/usr/share/nginx/html`.
- [x] `docker compose -f docker-compose.yml -f docker-compose.prod.yml config`
      показывает `build.dockerfile = nginx/Dockerfile.prod` для nginx, порты
      80/443, тома /etc/letsencrypt и /var/www/certbot сохранены.
- [x] `deploy.yml` собирает SPA на runner с `REACT_APP_API_URL=/api` и удаляет
      `node_modules` перед scp (на сервер не копируются сотни МБ).
- [x] Локальная разработка не меняется: `nginx/Dockerfile` (multi-stage build)
      остаётся прежним.
- [x] YAML валиден, `docker build -f nginx/Dockerfile.prod` собирается, внутри
      образа есть `index.html` и `static/`.

## Выполненные действия

- **nginx/Dockerfile.prod**: тонкий образ `nginx:1.25-alpine` + `nginx.conf` +
  `frontend/build` → `/usr/share/nginx/html`. Без npm/react — сборка не требует
  CPU/RAM сервера и не тянет образ node:18.
- **docker-compose.prod.yml**: nginx переведён на `build.dockerfile:
  nginx/Dockerfile.prod` (контекст — корень репозитория, как и раньше).
- **.github/workflows/deploy.yml**: перед scp добавлены шаги Setup Node.js
  (cache npm), `npm ci`, `npm run build` с `REACT_APP_API_URL=/api` (иначе
  API-вызовы улетали бы на `http://localhost:8000`), `rm -rf frontend/node_modules`
  — на сервер едут только исходники и готовые статики.
- **deploy/DEPLOY.md**: описан новый поток и причина ускорения.

## Изменённые файлы

- `nginx/Dockerfile.prod` (новый)
- `docker-compose.prod.yml`
- `.github/workflows/deploy.yml`
- `deploy/DEPLOY.md`
- `docs/agent/tasks/T-071/20260907T121742Z_opencode_a01.md`

## Команды и результаты

```text
Команда: docker compose -f docker-compose.yml -f docker-compose.prod.yml config
Результат: nginx build.dockerfile = nginx/Dockerfile.prod; ports 80:80, 443:443;
 volumes /etc/letsencrypt (ro), /var/www/certbot. OK

Команда: python yaml.safe_load(deploy.yml, docker-compose.prod.yml)
Результат: YAML OK

Команда: docker build -f nginx/Dockerfile.prod -t finbalance-nginx-prod:test .
Результат: собран за <1 мин (без network-heavy стадии), образ создан

Команда: docker run --rm finbalance-nginx-prod:test ls /usr/share/nginx/html
Результат: index.html, static/, asset-manifest.json и др. — статики на месте

Команда: rg dockerignore
Результат: .dockerignore отсутствует — COPY frontend/build не игнорируется
```

## Блокеры и риски

- SPA на runner собирается с `REACT_APP_API_URL=/api` — это обязательно,
  иначе собранные статики будут ходить в `http://localhost:8000`.
- Backend/bot по-прежнему собираются на сервере (`pip install`), но с docker
  layer-cache повторные сборки быстрые; основной тормоз — frontend — убран.
  Дальнейшая оптимизация (если понадобится): перенос всех образов в GHCR
  (потребует PAT с read:packages для приватного репозитория).

## Точный следующий шаг

Коммит, push ветки `feature/T-071-deploy-speed-frontend`, создание PR.

## SUBAGENT_HANDOFF

**Статус:** IN_PROGRESS
**Выполненные критерии:** 5/5
**Оставшиеся критерии:** замер времени деплоя на реальном сервере
**Изменённые файлы:** перечислены выше
**Команды и результаты:** compose config — Dockerfile.prod/80/443 OK; YAML OK;
 docker build Dockerfile.prod успешно; статики в образе на месте
**Блокеры:** нет
**Точный следующий шаг:** коммит, push, PR