# Файл выполнения задачи

ID выполнения: 20260907T102030Z_opencode_a01
Задача: T-069
Исполнитель: opencode
Начало: 2026-09-07T10:20:30Z
Предыдущая попытка: нет
Git HEAD до работы: 4ec40de (main)
Статус: IN_PROGRESS

## Цель выполнения

Не учитывать в расходах переводы самому себе: если в описании операции Имя, Отчество и
первая буква фамилии получателя перевода совпадают с Именем, Отчеством и первой буквой
фамилии владельца выписки (User.name) — такой перевод помечается `is_self_transfer=True`,
сохраняется в БД, но исключается из агрегатов расходов (total_amount, by_category,
top_payees, динамика, семейные суммы) и скрывается из списков операций во фронтенде.

## Разрешённая область

- `backend/app/services/names.py` (новый модуль парсинга имён и детекции self-transfer)
- `backend/app/services/transactions.py`
- `backend/app/services/analytics.py`
- `backend/app/models/models.py`
- `backend/alembic/versions/` (миграция 008)
- `backend/app/categorization/categorizer.py`
- `backend/app/routers/transactions.py`, `backend/app/routers/bot.py`
- `frontend/src/pages/Dashboard.js`, `frontend/src/pages/Operations.js`
- `tests/test_self_transfers.py` (новый)
- `docs/agent/tasks/T-069/`
- `TASKS.md`

## Запрещено

- Несвязанные изменения.
- Новые зависимости без одобрения.
- Дублировать прогресс в `PLAN.md`.
- Добавлять функции, которых нет в `PRODUCT.md`.

## Проверяемые критерии

- [ ] Перевод самому себе при совпадении «Имя + Отчество + первая буква фамилии» помечается `is_self_transfer=True` при импорте (web и bot).
- [ ] Расходы в сводке (analytics) не учитывают переводы самому себе.
- [ ] Обычные переводы и покупки не помечаются ошибочно.
- [ ] Перевод самому себе остаётся в БД и в списке операций (поле `is_self_transfer`), но скрывается из списков во фронтенде.
- [ ] pytest зелёный (новые тесты), ruff чист по изменённым файлам, `npm run check` — Compiled successfully.

## Команда запуска тестов (локально)

```text
DATABASE_URL="sqlite:///:memory:" .venv/Scripts/python.exe -m pytest -q
.venv/Scripts/ruff.exe check backend/app/services/names.py ...
cd frontend && npm run check
```