# Файл выполнения задачи

ID выполнения: 20260905T064955Z_opencode_a01
Задача: T-010
Исполнитель: opencode
Начало: 2026-09-05T06:49:55Z
Предыдущая попытка: нет
Git HEAD до работы: 4cfe23d (origin/main)
Статус: DONE

## Цель выполнения

Создать сервис сохранения транзакций: запись `Transaction` в БД с привязкой
к `family_id` и `user_id`, а также обработку дублей (в пределах одного импорта
и повторных импортов той же выписки).

## Разрешённая область

- `backend/app/services/` — новый модуль сохранения транзакций.
- `backend/app/routers/transactions.py` — новый роутер с эндпоинтом импорта.
- `backend/main.py` — подключение роутера.
- `tests/`
- `TASKS.md`
- `docs/agent/tasks/T-010/`

## Запрещено

- Изменения в `backend/app/parsers/`, `backend/app/categorization/` (вне области).
- Изменения в существующих моделях без необходимости.
- Несвязанные изменения и новые зависимости.

## Проверяемые критерии

- [x] При сохранении создаётся запись `Transaction` с корректными `family_id`, `user_id`, датой, суммой и описанием.
- [x] Категория из категоризатора резолвится в `Category` (fetch-or-create) и проставляется `category_id`.
- [x] Дубли в пределах одного импорта сохраняются один раз.
- [x] Повторный импорт той же выписки (те же дата+сумма+описание) не создаёт новые записи.

## Выполненные действия

- Ветка `feature/T-010-save-transactions` создана от `origin/main` (4cfe23d).
- Создан `backend/app/services/transactions.py`:
  - `save_transactions(db, *, family_id, user_id, transactions, source_file)` — сохраняет операции из выписки в БД;
  - ключ дубликата: дата + сумма (2 знака) + исходное описание;
  - дубли внутри одного импорта схлопываются, повторный импорт сверяется с записями семьи;
  - несуществующая категория создаётся автоматически (`fetch-or-create`);
  - `SaveResult` возвращает `total / created / duplicates_skipped`.
- Создан `backend/app/services/__init__.py`.
- Создан `backend/app/routers/transactions.py`:
  - `POST /api/families/{family_id}/transactions/import` — принимает CSV, парсит (`parse_csv_bytes`), категоризирует, сохраняет через сервис; проверяет членство в семье (403);
  - `GET /api/families/{family_id}/transactions` — список транзакций семьи с именем категории.
- `backend/main.py`: подключён `transactions.router` с префиксом `/api`.
- `tests/conftest.py`: добавлены фикстуры `session_factory` и `db` (SQLite in-memory, `StaticPool`); компайлер `postgresql.UUID` → `CHAR(36)` для SQLite.
- Созданы тесты:
  - `tests/test_transaction_service.py` — 8 unit-тестов сервиса сохранения и обработки дублей;
  - `tests/test_transaction_router.py` — 5 integration-тестов эндпоинтов через `TestClient` с переопределением `get_db`.
- Тесты: `python -m pytest tests -q` → 57 passed.
- `ruff check` по изменённым файлам — All checks passed.
- Оставшиеся ошибки ruff (в `models.py`, `alembic/*`, `database.py`) — предсуществующие, в файлах вне области задачи.

## Изменённые файлы

- `backend/app/services/__init__.py` — новый.
- `backend/app/services/transactions.py` — новый.
- `backend/app/routers/transactions.py` — новый.
- `backend/main.py` — подключение роутера transactions.
- `tests/conftest.py` — фикстуры для SQLite.
- `tests/test_transaction_service.py` — новый.
- `tests/test_transaction_router.py` — новый.
- `TASKS.md` — статус T-010 → DONE.
- `docs/agent/tasks/T-010/20260905T064955Z_opencode_a01.md` — файл выполнения.

## Команды и результаты

```text
Команда: python -m pytest tests -q
Результат: 57 passed, 6 warnings (DeprecationWarning httpx TestClient 'app')

Команда: python -m ruff check backend/app/services backend/app/routers/transactions.py backend/main.py tests/conftest.py tests/test_transaction_service.py tests/test_transaction_router.py
Результат: All checks passed
```

## Блокеры и риски

- `ruff check backend bot` на CI падает из-за предсуществующих ошибок в `backend/app/models/*`, `backend/alembic/*`, `backend/app/database.py` (вне области T-010, не исправлялись).
- Дубликат определяется по дате+сумме+описанию; для операций с одинаковыми значениями в один день (например, две одинаковые покупки в одной точке) возможен ложный пропуск. Для MVP приемлемо, при необходимости можно добавить уникальный признак (MCC/номер авторизации).

## Точный следующий шаг

Задача завершена: сформировать коммиты, запушить ветку и создать PR на проверку координатором.