# T-040 Попытка a02 — Кнопка в правый верхний угол окна «Операции»

Владелец: opencode
Дата: 2026-09-06T05:22:55Z
Статус: IN_PROGRESS
Предыдущая попытка: `docs/agent/tasks/T-040/20260906T051557Z_opencode_a01.md`
Git HEAD до работы: eecb4a9256532b8a8ca0540a18ac997687d004f5

## Цель выполнения

Переместить кнопку-переключатель «Убрать пополнения» из-под таблицы в правый
верхний угол блока «Операции» (на строку заголовка). Кнопка видна всегда, когда
в блоке есть транзакции.

## Разрешённая область

- `frontend/src/pages/Dashboard.js`
- `frontend/src/styles.css`
- `docs/agent/tasks/T-040/`

## Запрещено

- Несвязанные изменения.
- Новые зависимости без одобрения.
- Изменение backend-логики. Условия рендера блока операций не меняем.

## Проверяемые критерии

- [ ] Кнопка находится в правом верхнем углу окна «Операции» (рядом с заголовком).
- [ ] Поведение переключателя сохранено: скрывает/возвращает пополнения.
- [ ] `npm run check` — Compiled successfully.
- [ ] Правки закоммичены и запушены в ветку `feature/T-040-hide-incomes`.

## Выполненные действия

1. `Dashboard.js`: кнопка-переключатель перенесена из-под таблицы в правый верхний
   угол блока «Операции» — размещена в `div.ops-header` рядом с заголовком
   «Операции (N)» (заголовок слева, кнопка справа). Убран нижний блок
   `.ops-filter-actions`, подпись «…кнопкой ниже» заменена на «…кнопкой выше».
2. `styles.css`: `.ops-filter-actions` заменён на `.ops-header`
   (flex, space-between, align-items: center, margin-bottom), `.ops-header h2 { margin: 0 }`.
3. Поведение переключателя не менялось — только позиционирование.
4. `npm run check` — Compiled successfully.
5. Причина «кнопка отсутствует» у пользователя: PR #48 не влит в main, поэтому на
   ветке main фичи нет. Правки вносятся в ветку `feature/T-040-hide-incomes`.

## Изменённые файлы

- `frontend/src/pages/Dashboard.js`
- `frontend/src/styles.css`
- `frontend/build/*` (пересобраны `npm run check`)

## Команды и результаты

```text
Команда: npm run check
Результат: Compiled successfully.
```

## Блокеры и риски

Нет. Ручная проверка в браузере не проводилась (нет запущенного браузера);
поведение фильтра проверено на реальных данных в попытке a01, в этой итерации
изменено только позиционирование кнопки.

## Точный следующий шаг

Ревью PR #48 координатором.

## SUBAGENT_HANDOFF

**Статус:** DONE
**Выполненные критерии:** 4/4
**Оставшиеся критерии:** нет
**Изменённые файлы:** см. выше
**Команды и результаты:** см. выше
**Блокеры:** нет
**Точный следующий шаг:** ревью PR #48