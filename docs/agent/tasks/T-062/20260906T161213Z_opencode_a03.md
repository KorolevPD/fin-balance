# T-062: Разрешение конфликтов PR #74 (feature/avatar-default-image)

**Попытка:** a03
**Владелец:** opencode
**Начало:** 2026-09-06T16:12:13Z

## Предыдущие попытки

- a01 (`20260906T114647Z_opencode_a01.md`): реализация фичи «Дефолтный аватар
  при отсутствии фото профиля».
- a02 (`20260906T124000Z_opencode_a02.md`): фикс отображения даты регистрации
  (ветка `fix/registration-date-display-v2`), уже влит в main.

## Задача

Исправить конфликты в PR #74, ветка `feature/avatar-default-image`.

## Диагностика

`gh pr view 74` показал `mergeStateStatus: DIRTY`, `mergeable: CONFLICTING`.

Ветка была основана на старом main. При слиянии свежего `origin/main`
(в т.ч. PR #77 «Выйти из семьи», зелёные кнопки, фикс даты регистрации,
скрытие доходов) конфликты возникли **только** в собранных артефактах
`frontend/build/*` (rename/rename JS/CSS-бандлов, asset-manifest.json,
index.html). Исходники автослились чисто:

- `Profile.js` — дефолтный аватар (`src={preview || '/avatar.jpg'}` из T-062) +
  кнопка «Выйти из семьи» (`handleLeave` из main) + фикс даты регистрации
  (`formatDate(...) || user[...]`, `type: 'text'`);
- `Layout.js` — дефолтный аватар в шапке (`src={preview || '/avatar.jpg'}`);
- `styles.css` — зелёные кнопки `#24a647`/`#1a8a39` из main;
- `Dashboard.js` — скрытие доходов из main.

Backend (`families.py`, `ai.py`, тесты) и docs не были затронуты конфликтами.

## Что сделано

1. `git checkout feature/avatar-default-image`, `git merge origin/main`.
2. Из индекса сняты конфликтующие build-артефакты (`frontend/build`),
   удалена build-директория.
3. Фронтенд пересобран из согласованных исходников:
   `npm run check` — Compiled successfully;
   новые артефакты `main.77852943.js`, `main.17434ec4.css`, `build/avatar.jpg`.
4. Проверка согласованности:
   - маркеров конфликтов (`<<<<<<<`) нет в `src`, `public`, `build`;
   - бандл содержит `avatar.jpg` (T-062), `leave` (T-064 из main), зелёные
     кнопки `#24a647`/`#1a8a39`;
   - `Profile.js`/`Layout.js` содержат `src={preview || '/avatar.jpg'}`;
   - `public/avatar.jpg` и `build/avatar.jpg` проиндексированы.
5. Закоммичено слияние `175f742`.

## Проверка (доказательства)

- `pytest tests/ -q` — **159 passed**.
- `ruff check` по изменённым backend-файлам — All checks passed.
- `npm run check` — Compiled successfully.

## Точный следующий шаг

Push `175f742` в `origin/feature/avatar-default-image`, проверка, что PR #74
стал mergeable, затем запрос ревью координатора.
