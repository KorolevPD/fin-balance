# Файл выполнения задачи

ID выполнения: 20260907T111500Z_main_a03
Задача: T-068
Исполнитель: main
Начало: 2026-09-07T11:15:00Z
Предыдущая попытка: docs/agent/tasks/T-068/20260907T101500Z_main_a02.md
Git HEAD до работы: c544059
Статус: IN_PROGRESS

## Цель выполнения

Настроить HTTPS (Let's Encrypt через certbot) для деплоя: nginx отдаёт сайт по 443 с SSL-сертификатом, HTTP 80 переадресует на HTTPS. Пользователю даны точные команды для сервера.

## Разрешённая область

- `nginx/nginx.conf`, `nginx/Dockerfile`
- `docker-compose.prod.yml`
- `deploy/deploy.sh`, `deploy/DEPLOY.md`
- `.github/workflows/deploy.yml` (передача домена в скрипт)
- `docs/agent/tasks/T-068/`

## Запрещено

- Несвязанные изменения.
- Новые зависимости без одобрения.

## Проверяемые критерии

- [ ] nginx.conf содержит SSL-сервер (443) и переадресацию 80→443 с `/.well-known/acme-challenge/`.
- [ ] docker-compose.prod.yml монтирует `/etc/letsencrypt` и `/var/www/certbot` в nginx.
- [ ] deploy.sh выпускает/обновляет сертификат через certbot (standalone bootstrap до первого `up`, затем `certbot renew`).
- [ ] deploy/DEPLOY.md описывает команды для сервера по выпуску и обновлению сертификата.
- [ ] YAML/config валидны, `npm run check` не требуется (фронт не менялся).

## Выполненные действия

- `nginx/nginx.conf`: сервер на 443 ssl (сертификаты `/etc/letsencrypt/live/finbalance/{fullchain,privkey}.pem`), HTTP 80 отдаёт `/.well-known/acme-challenge/` и переадресует всё остальное на HTTPS.
- `docker-compose.prod.yml`: nginx публикует порт 443 (`DEPLOY_PORT_SSL`), монтирует `/etc/letsencrypt` (ro) и `/var/www/certbot`.
- `deploy/deploy.sh`: если задан `DEPLOY_DOMAIN` и сертификата нет — выпуск `certbot certonly --standalone` до запуска стека; если сертификат есть — `certbot renew` с временной остановкой nginx, затем reload. Поддержка аргументов 8–9 (`DEPLOY_DOMAIN`, `CERTBOT_EMAIL`).
- `.github/workflows/deploy.yml`: передача секретов `DEPLOY_DOMAIN` и `CERTBOT_EMAIL` в скрипт.
- `deploy/DEPLOY.md`: раздел «HTTPS (Let's Encrypt)», новые секреты, установка certbot на сервере.

## Изменённые файлы

- `nginx/nginx.conf`
- `docker-compose.prod.yml`
- `deploy/deploy.sh`
- `deploy/DEPLOY.md`
- `.github/workflows/deploy.yml`
- `docs/agent/tasks/T-068/20260907T111500Z_main_a03.md`

## Команды и результаты

```text
Команда: python -c "yaml.safe_load(deploy.yml); yaml.safe_load(docker-compose.prod.yml)"
Результат: YAML OK

Команда: bash -n deploy/deploy.sh
Результат: BASH OK
```

## Блокеры и риски

- Первичный выпуск сертификата выполняет `certbot certonly --standalone`: порт 80 на сервере должен быть свободен на момент первого деплоя (nginx останавливается при необходимости).
- `--email "${CERTBOT_EMAIL}"`: при пустом email certbot потребует ввода — секрет CERTBOT_EMAIL нужно заполнить.

## Точный следующий шаг

Закоммитить правки, запушить, пользователю: установить certbot на сервере, заполнить секреты `DEPLOY_DOMAIN` и `CERTBOT_EMAIL` в GitHub, перезапустить Deploy.

## SUBAGENT_HANDOFF

**Статус:** IN_PROGRESS
**Выполненные критерии:** 5/5
**Оставшиеся критерии:** проверка на реальном сервере после деплоя
**Изменённые файлы:** перечислены выше
**Команды и результаты:** YAML OK, bash OK
**Блокеры:** нет
**Точный следующий шаг:** commit + push, команды пользователю